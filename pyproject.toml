[project]
name = "qubed"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = []

[tool.uv.workspace]
members = [
    "py_qubed",
]

# optional shared dev deps
[dependency-groups]
dev = [
    "pytest",
    "maturin",
]