[project]
name = "qubed"
version = "0.4.4"
requires-python = ">=3.8"
dependencies = []

[tool.uv.workspace]
members = [
    "py_qubed",
]

# optional shared dev deps
[dependency-groups]
dev = [
    "pytest",
    "maturin",
]